services:
  avianvisitors:
    build:
      context: .
      dockerfile: docker/Dockerfile
    image: avianvisitors:latest
    container_name: avianvisitors
    restart: unless-stopped
    ports:
      - "127.0.0.1:80:8080"
      - "127.0.0.1:8081:8081" # public gallery, only enable if wanted.
    devices:
      - "/dev/snd:/dev/snd"
    group_add:
      - audio
    environment:
      BIRDNET_REC_CARD: "default"
      BIRDNET_CHANNELS: "2"
      BIRDNET_LATITUDE: ""
      BIRDNET_LONGITUDE: ""
      AV_ADMIN_USER: ""
      AV_ADMIN_PASSWORD: ""
      AV_PUBLIC_GALLERY: "false"
      AV_PUBLIC_PATH: ""
    volumes:
      - data:/data

volumes:
  data:
